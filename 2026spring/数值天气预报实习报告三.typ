#import "../book.typ": book-page
#show: book-page.with(title: "NWP Practice Report 3")
#outline()

= 基本原理

本次实习以 1973 年 4 月 29 日 08 时（北京时）我国东北、华北地区 500hPa 等压面位势高度场及地转风场作为初值，采用固定的水平侧边界条件，应用正压原始方程二次守恒平流格式的模式，制作未来 24 小时有限区域 500hPa 位势高度场和风场的预报。

== 正压原始方程模式

正压原始方程模式是最简单的大气动力学数值模式之一。正压大气假定大气密度仅为气压的函数，等压面与等密度面重合，且水平风速不随高度变化。该模式在 500hPa 等压面上求解水平动量方程和连续方程，能够描述 Rossby 波传播、槽脊移动等中纬度天气尺度过程的基本特征。

模式采用 Lambert 保角投影，计算区域为 $20 times 16$ 个格点，水平格距 $d = 300"km"$，中心经纬度为 $(51 degree "N", 118 degree "E")$，时间步长 $Delta t = 600"s"$。模式使用二次守恒平流格式保证非线性计算的稳定性，采用 Euler 后差和中央差的交替积分方案：先在每个 12 小时循环的前 1 小时采用 Euler 后差格式起步，随后改用中央差蛙跳格式。

== 地转风初值

在实际数值天气预报中，观测资料通常只包含位势高度场，风场需要通过地转近似由高度场计算得到。地转风关系给出了大尺度运动中气压梯度力与科里奥利力之间的平衡：

$
u_g = -frac(m g, f) frac(dif z, dif y)
$

$
v_g = frac(m g, f) frac(dif z, dif x)
$

其中 $m$ 为地图放大系数，$f$ 为科里奥利参数，$g = 9.8 "m/s"^2$ 为重力加速度，$z$ 为位势高度。

在差分计算中，采用二阶中心差分近似空间导数：

$
u_(i,j) = -frac(m_(i,j) g, f_(i,j)) dot (z_(i,j+1) - z_(i,j-1)) / (2 d)
$

$
v_(i,j) = frac(m_(i,j) g, f_(i,j)) dot (z_(i+1,j) - z_(i-1,j)) / (2 d)
$

计算仅在内点 $(i = 2, ..., m-1; j = 2, ..., n-1)$ 上进行，边界值保持原输入值不变。

== 五点平滑

数值模式中的非线性作用和计算误差会在格点场中产生高频噪声（短波分量），这些噪声不仅影响预报精度，还可能导致计算不稳定。五点平滑是一种常用的空间滤波方法，通过将每个格点值与其四个邻近格点值进行加权平均来滤除短波。

五点平滑公式为：

$
tilde(F)_(i,j)^(x y) &= F_(i,j) + S/4 (F_(i+1,j) + F_(i-1,j) + F_(i,j+1) + F_(i,j-1) - 4F_(i,j)) \
&= (1 - S) F_(i,j) + S (F_(i+1,j) + F_(i-1,j) + F_(i,j+1) + F_(i,j-1)) / 4
$

其中 $S = 0.5$ 为平滑系数。当 $S = 0.5$ 时，平滑算子可以有效滤除 2 倍格距波（Nyquist 波），而对长波的振幅影响较小。该公式的含义是将 $S$ 权重的邻点平均值与原值按照 $(1-S):S$ 的比例进行混合。

== 侧边界条件

由于本模式模拟的是有限区域，区域边界上无法进行正常的中央差计算，因此采用固定的水平侧边界条件：在整个积分过程中，区域最外一圈格点的值保持不变（等于初始值）。这种处理方式虽然简单，但会在边界附近产生一定的计算误差。为减轻这一问题，模式在积分过程中对边界附近的点使用了边界平滑处理。

= 子程序设计

== 地转风初值子程序 cgw

本子程序利用位势高度场通过地转风关系计算初始风场的 $u$、$v$ 分量。程序遍历所有内点 $(i=2,...,m-1; j=2,...,n-1)$，使用中心差分计算高度场梯度，结合地图放大系数 $m$ 和科里奥利参数 $f$ 得到地转风。

本子程序关键代码（Fortran）如下：

```fortran
subroutine cgw(ua,va,za,rm,f,d,m,n)
  dimension ua(m,n),va(m,n),za(m,n),rm(m,n),f(m,n)
  g = 9.8
  c = 1.0/(2.0*d)
  m1 = m-1
  n1 = n-1
  do i = 2, m1
    do j = 2, n1
      ua(i,j) = -rm(i,j)*g/f(i,j) * (za(i,j+1)-za(i,j-1)) * c
      va(i,j) =  rm(i,j)*g/f(i,j) * (za(i+1,j)-za(i-1,j)) * c
    enddo
  enddo
  return
end
```

== 五点平滑子程序 ssip

本子程序对全场内点进行 $l$ 次五点平滑，每次平滑先计算平滑结果存入工作数组 $w$，再将 $w$ 的值回代到原数组 $a$。程序使用参数 $l$ 控制平滑次数（本模式中 $l=2$，即每 12 小时结束时进行 2 次平滑）。

```fortran
subroutine ssip(a,w,s,m,n,k,l)
  dimension a(m,n),w(m,n)
  m1 = m-1
  n1 = n-1
  do kk = 1, l
    do i = 2, m1
      do j = 2, n1
        w(i,j) = a(i,j) + s/4.0 * (a(i+1,j)+a(i-1,j)+a(i,j+1)+a(i,j-1)-4.0*a(i,j))
      enddo
    enddo
    do i = 2, m1
      do j = 2, n1
        a(i,j) = w(i,j)
      enddo
    enddo
  enddo
  return
end
```

= 结果与讨论

== 程序运行

将编写完成的 `cgw` 和 `ssip` 子程序补充到 `mbpm1.f90` 中，并修正了文件路径和缺失的输出文件 OPEN 语句。程序在 macOS 环境下使用 gfortran 编译通过，分别以 `ni=0`（使用给定风场）和 `ni=1`（使用地转风初始化）两种模式运行，均顺利完成 24 小时预报计算。

== 初始场分析

1973 年 4 月 29 日 08 时 500hPa 初始位势高度场（图 1 左）显示，我国东北、华北地区处于一宽广的低压槽区，位势高度值范围在 5180—5880 gpm 之间。等高线呈西南-东北走向，表明槽线大致位于华北至东北西部地区。风场以偏西气流为主，与等高线的走向基本吻合，符合地转风关系。

初始风场的 $u$ 分量范围为 $-21.94$ 到 $36.43 "m/s"$，$v$ 分量范围为 $-40.49$ 到 $29.98 "m/s"$，风速量级与 500hPa 典型风场一致。

== 24 小时预报结果

#figure(
  image("数值天气预报/forecast_result.png", width: 100%),
  caption: [500hPa 位势高度场和风场：初始场（左）与 24h 预报场（右）对比]
)

图 1 展示了 24 小时预报结果与初始场的对比。预报场中，24h 预报高度场范围为 5180—5880 gpm，与初始场范围相同，这与固定的侧边界条件有关——边界格点值在积分过程中保持不变，限制了区域内高度场极值的变化。

风场方面，24 小时预报的 $u$ 分量范围为 $-10.27$ 到 $26.32 "m/s"$，$v$ 分量范围为 $-18.71$ 到 $29.86 "m/s"$。可以看到，预报风速较初始场有一定程度的减小，这可能是数值耗散和平滑处理共同作用的结果。

从形势看，预报高度场保持了初始场的总体槽脊格局，槽线位置变化不大，符合正压模式的基本特征——在没有斜压能量转换机制的情况下，系统的移动和发展主要受 Rossby 波频散控制。固定边界条件也在一定程度上抑制了系统的自由传播。

== 地转风初始化的影响

对比 `ni=0`（使用给定风场）和 `ni=1`（地转风初始化）两种模式的输出，当使用地转风初始化时，初始风场完全由高度场通过地转关系导出，风速和风向与等高线的关系严格满足地转平衡。这种方式能保证初始时刻地转适应的快速过程不产生虚假的重力惯性波。

然而实际大气中的风场与严格的地转风存在偏差（非地转风分量），这些偏差对应着气流的辐散辐合，是天气系统发展的重要动力学信号。使用给定的实测风场作为初值更接近实际大气状态。

五点平滑在模式中起到关键作用。每次平滑处理可以有效抑制短波噪声，防止计算不稳定。参数 $S=0.5$ 时能完全滤除 2 倍格距波，且平滑两次可进一步降低中短波分量的虚假增长，对维持模式长时间稳定积分至关重要。

= 结论

本次实习通过编写地转风初值子程序和五点平滑子程序，完成了正压原始方程模式的构建和 24 小时预报试验。主要结论如下：

1. *地转风初值子程序*：利用中心差分格式从位势高度场计算地转风场，实现了风场的静力初始化。当地转风近似成立时，该初始化方式可以有效减少初始时刻重力惯性波的不真实振荡。

2. *五点平滑子程序*：实现了对格点场的空间滤波，通过 $S=0.5$ 的平滑系数有效滤除 2 倍格距短波，提高了模式的数值稳定性。模式中结合边界平滑和时间平滑，形成了一套完整的高频噪声控制方案。

3. *模式运行结果*：在两种初始化方式（直接使用给定风场和地转风初始化）下，模式均能稳定完成 24 小时预报计算，输出合理的 500hPa 位势高度场和风场。预报场保持了初始场的总体环流特征，风速因数值耗散而略有减弱。

4. *固定边界的影响*：有限区域的固定侧边界条件限制了系统自由传播和强度变化，这是本模式的一个基本约束。实际业务预报中常采用嵌套于全球模式的单向或双向嵌套方案来减小边界效应。

本次实习加深了对正压原始方程模式数值实现的理解，掌握了地转风初始化、五点平滑等数值天气预报基本技术的编程实现方法，为后续学习和应用更复杂的数值模式打下了基础。
